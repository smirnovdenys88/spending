server:
  port: 8080
spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/spending_ds
    username: user_ds
    password: pass_ds
    jpa:
      hibernate:
        ddl-auto: none
      properties:
        hibernate.show_sql: true